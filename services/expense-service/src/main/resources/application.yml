spring:
  datasource:
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    table: expense_flyway_schema_history

finfast:
  jwt:
    issuer: finfast-api

# Environment variables for local docker launch

SPRING_PROFILES_ACTIVE=dev
QUARKUS_PROFILE=dev

DATABASE_HOST=db

KAFKA_BOOTSTRAP_SERVERS=kafka:9092
KAFKA_USERNAME=kafka
KAFKA_PASSWORD=admin-client-secret

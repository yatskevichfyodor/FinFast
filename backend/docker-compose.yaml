services:
  backend:
    build: .
    container_name: finfast-backend
    ports:
      - "8081:8081"
    volumes:
      - finfast-data:/app/data

  # PostgreSQL — заготовка на будущее
  #
  # db:
  #   image: postgres:18
  #   container_name: finfast-db
  #   environment:
  #     POSTGRES_DB: finfast
  #     POSTGRES_USER: finfast
  #     POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
  #   volumes:
  #     - postgres-data:/var/lib/postgresql/data
  #   ports:
  #     - "5432:5432"

volumes:
  finfast-data:
  # postgres-data: